/**
 * @deprecated Marketplace logistics is modeled as channel fulfillment methods
 * (FBA / Easy Ship / FBF / NFBF), not as LogisticsAdapter catalog entries.
 *
 * Kept as stubs so historical imports do not break during the transition.
 * Do not register these in logisticsRegistry.
 */
export {};
