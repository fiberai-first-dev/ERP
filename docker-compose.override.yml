# Auto-loaded for `docker compose up` (local). Not used when you pass -f docker-compose.yml only.

services:
  db:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"

  backend:
    ports:
      - "4000:4000"

  frontend:
    ports:
      - "3000:80"
